import React, { Component } from 'react';

class Audio extends Component {
    render() {
        return (
            <div>
                <button>Play</button>
            </div>
        );
    }
}

export default Audio;
